V4.1: Rebuilt Parent Coach Match Day as Next Match, reading the existing saved-match registry. Next upcoming match selected, all saved matches selectable, format-specific scorecard navigation, lineup display, location/start time/warm-up/snacks/notes, native text sharing. Match Lineup now stores optional additional notes. Single-set and two-set scorecards auto-load the chosen match from Game Day. Timed scoring retains its existing native saved-match picker: select the same match there. Local-only test: secure roles and cross-device sync NOT implemented. No test data seeded.
